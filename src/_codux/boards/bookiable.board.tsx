import React from "react";
import { createBoard } from "@wixc3/react-board";

export default createBoard({
  name: "bookiable",
  Board: () => (
    <div>
      <button />
    </div>
  ),
  isSnippet: true,
});
